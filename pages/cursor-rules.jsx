import React from 'react'; export default function CursorRulesPage() { return ( <div className="max-w-4xl mx-auto px-4 py-12 font-sans text-gray-900 bg-white"> {/* Header Section */} <header className="mb-12 border-b border-gray-200 pb-8"> <span className="text-blue-600 font-semibold tracking-wide uppercase text-sm">Next.js & Cursor Recipes</span> <h1 className="text-4xl font-extrabold tracking-tight text-gray-900 mt-2 sm:text-5xl"> The Ultimate Cursor Rule for Clean React.js </h1> <p className="mt-4 text-xl text-gray-500"> Stop AI from writing messy, outdated React code. Use this configuration file to force Cursor to generate production-ready code. </p> </header> {/* The Problem & Solution Section */} <section className="mb-12 space-y-6"> <h2 className="text-2xl font-bold text-gray-800">The Problem</h2> <p className="text-gray-600 leading-relaxed text-lg"> By default, AI tools like Cursor and Copilot mix modern React with old patterns from 2019, use bad state habits, or write inefficient functions that slow down your app. </p> <h2 className="text-2xl font-bold text-gray-800 pt-4">The Solution</h2> <p className="text-gray-600 leading-relaxed text-lg"> Create a file in your project root at <code className="bg-gray-100 px-2 py-1 rounded text-red-600 font-mono text-base">.cursor/rules/react-modern.mdc</code> and paste the configuration below inside it. </p> </section> {/* The Recipe Code Box */} <section className="mb-12"> <div className="flex justify-between items-center bg-gray-800 text-gray-300 px-4 py-2 rounded-t-lg font-mono text-sm"> <span>react-modern.mdc</span> <span className="text-xs text-gray-400">Copy this text</span> </div> <pre className="bg-gray-900 text-green-400 p-6 rounded-b-lg overflow-x-auto font-mono text-sm leading-relaxed shadow-lg"> {`--- description: Rules for generating modern and clean React.js components globs: src/**/*.js, src/**/*.jsx, components/**/*.jsx --- # Modern React.js Rules - **Functional Only:** Never generate class components. Use arrow functions. - **Export Pattern:** Use named exports instead of default exports. - **Props:** Always destructure props directly in the component signature. - **Keys:** Always provide a stable, unique key prop when rendering arrays. Never use the array index.`} </pre> </section> {/* Before and After Comparison Section */} <section className="mb-12"> <h2 className="text-2xl font-bold text-gray-800 mb-6">Why it Matters (Before vs After)</h2> <div className="grid grid-cols-1 md:grid-cols-2 gap-8"> {/* Before */} <div className="border border-red-200 rounded-lg p-6 bg-red-50"> <h3 className="text-red-700 font-bold mb-3 flex items-center">❌ Standard AI Output (Messy)</h3> <pre className="text-xs font-mono text-red-900 bg-white p-4 rounded border border-red-100 overflow-x-auto"> {`export default function Dashboard(props) { const user = props.user; return ( <div style={{ padding: '20px' }}> {user.items.map((item, index) => ( <li key={index}>{item.name}</li> ))} </div> ); }`} </pre> </div> {/* After */} <div className="border border-green-200 rounded-lg p-6 bg-green-50"> <h3 className="text-green-700 font-bold mb-3 flex items-center"> Clean Code (With Rule)</h3> <pre className="text-xs font-mono text-green-900 bg-white p-4 rounded border border-green-100 overflow-x-auto"> {`export const Dashboard = ({ user }) => { return ( <div className="p-5"> {user.items.map((item) => ( <li key={item.id}>{item.name}</li> ))} </div> ); };`} </pre> </div> </div> </section> </div> ); }
